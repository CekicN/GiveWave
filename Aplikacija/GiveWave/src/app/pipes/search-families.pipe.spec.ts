import { SearchFamiliesPipe } from './search-families.pipe';

describe('SearchFamiliesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFamiliesPipe();
    expect(pipe).toBeTruthy();
  });
});
